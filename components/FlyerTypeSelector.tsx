// This component is no longer used in the single-page studio layout.
// It is kept as an empty valid module to prevent build errors from stale imports.
import React from 'react';

const FlyerTypeSelector: React.FC = () => {
  return null;
};

export default FlyerTypeSelector;
